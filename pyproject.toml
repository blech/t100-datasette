[project]
name = "t100"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasette>=0.65.2",
    "datasette-geojson>=0.4.0",
    "datasette-vega>=0.6.2",
    "sqlite-utils>=3.39",
]

[tool.uv.sources]
datasette = { path = "../datasette", editable = true }
